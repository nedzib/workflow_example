<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Workflow Example" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-slate-50 min-h-screen">
    <header class="bg-white border-b border-slate-200 shadow-sm">
      <div class="container mx-auto px-5 py-4 flex items-center justify-between">
        <h1 class="text-xl font-semibold"><%= link_to "Workflow Demo", root_path, class: "text-slate-900" %></h1>
        <nav class="flex items-center gap-4 text-sm">
          <% if user_signed_in? %>
            <span class="text-slate-600">Sesión iniciada como <%= current_user.email %></span>
            <% if current_user.admin? %>
              <%= link_to "Tablero de workflows", admin_workflows_path, class: "font-medium text-blue-600 hover:text-blue-500" %>
            <% end %>
            <%= button_to "Cerrar sesión", destroy_user_session_path, method: :delete, class: "text-red-600" %>
          <% else %>
            <%= link_to "Iniciar sesión", new_user_session_path, class: "text-blue-600" %>
            <%= link_to "Registrarse", new_user_registration_path, class: "text-blue-600" %>
          <% end %>
        </nav>
      </div>
    </header>
    <main class="container mx-auto mt-12 px-5">
      <% if notice.present? %>
        <div class="mb-4 rounded border border-green-200 bg-green-50 p-3 text-green-800"><%= notice %></div>
      <% end %>
      <% if alert.present? %>
        <div class="mb-4 rounded border border-red-200 bg-red-50 p-3 text-red-800"><%= alert %></div>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
